<h1>新規タスク入力ページ</h1>

<%= form_with(model: @task, local: true)do |f| %>
<%= f.label :content, "タスク" %>
<%= f.text_field :content %>
<%= f.submit "投稿"%>
<% end %>

<p><%= link_to "もどる", tasks_path %></p>